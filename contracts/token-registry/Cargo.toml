[package]
name = "token-registry"
version = "0.0.1"
edition = "2021"

[dependencies]
soroban-sdk = "26"

[dev-dependencies]
soroban-sdk = { version = "26", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
